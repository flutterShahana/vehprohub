import 'package:flutter/material.dart';

class Screen2map extends StatefulWidget {
  const Screen2map({Key? key}) : super(key: key);

  @override
  State<Screen2map> createState() => _Screen2mapState();
}

class _Screen2mapState extends State<Screen2map> {
  @override
  Widget build(BuildContext context) {
    return Scaffold(

      appBar: AppBar(

        title: Text('map'),

      ),

    );
  }
}
