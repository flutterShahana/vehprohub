import 'package:flutter/material.dart';

class Paymenthis extends StatelessWidget {
  const Paymenthis({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(

      appBar: AppBar(

        title: Text('Paymenthistory Page'),

      ),


    );
  }
}

